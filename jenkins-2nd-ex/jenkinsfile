pipeline {
    agent any

    stages {

        stage('Uptime') {
            steps {
                sh 'uptime'
            }
        }

        stage('RAM Usage') {
            steps {
                sh 'free -h'
            }
        }

        stage('Disk Usage') {
            steps {
                sh 'df -h'
            }
        }

    }
}